name: Pipeline de Qualidade
on: [push, pull_request]
jobs:
  testes:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Instalar Node.js
        uses: actions/setup-node@v4
      - name: Rodar Testes
        run: echo "Simulando execução de testes unitários..."
